# 같이살림 - Design.md

## 1. 디자인 방향

한 번 정하면 잘 바뀌지 않는 기준. 화면 작업 시 항상 참고.

### 디자인 시스템
Material Design 3 컴포넌트 구조 위에 **커스텀 소프트 파스텔 테마**를 얹는다. 버튼/카드/칩/바텀 내비 등 뼈대는 Material 3을 쓰되, 컬러·폰트·형태는 아래 파스텔 토큰으로 덮어쓴다. 홈 파스텔 시안이 시각 기준(source of truth)이다.

### 톤앤매너
따뜻하고 부드러운 파스텔 무드. 웜 크림 배경 위에 화이트 카드, 코랄·세이지·라벤더·민트의 파스텔 포인트로 커플·가정의 친근한 인상을 준다. 단, "가계부 앱"다운 신뢰감을 위해 채도는 과하지 않게, 정보 위계는 명확하게 유지한다. (이전의 미니멀 브라운·무장식 방향에서 전환됨)

### 컬러

| 역할 | 색상 | 설명 |
|---|---|---|
| Background | `#FBF3EA` | 웜 크림 앱 배경 |
| Card / Surface | `#FFFFFF` | 카드 배경 (배경과 대비되는 순백) |
| Text Primary (Ink) | `#4B403A` | 본문 텍스트 (웜 차콜) |
| Text Secondary (Muted) | `#A2948A` | 보조/메타 텍스트 |
| Line / Border | `#F0E6DA` | 구분선, 카드 내부 라인 |
| Primary (Coral) | `#E8896B` | 강조: 선택된 탭, 링크(전체보기), D-day 배지, 강조 수치 |
| Primary Soft | `#FCE4D8` | Coral의 옅은 버전: 아이콘 칩 배경, 프로필 배경 |
| Peach | `#F3A98E` | 진행률 그라데이션 시작색, 카테고리(식비) |
| Sage | `#9FBFA6` | 보조 포인트, 카테고리(카페) |
| Lavender | `#B7AEE0` | 보조 포인트, 카테고리(문화) |
| Mint | `#93C7B4` | 보조 포인트, 카테고리(여행) |
| Progress Track | `#F1E7DB` | 진행률 바 트랙 |

- 프라이머리는 Coral, 진행률 바는 Peach→Coral 그라데이션. 예산 100% 초과 시에만 경고색(테라코타 계열) 별도 적용.

#### 카테고리 색
카테고리마다 한 색을 가지며(`categories.color`), 리스트 배지·칩·홈 차트 어디서나 같은 색이다. **고정 항목 5개끼리는 색 계열이 겹치지 않는다** — 키가 달라도 눈에 비슷하게 보이면 겹침으로 본다.

파스텔 톤을 유지하되 위 파스텔 세트보다 한 단계 진한 색을 쓴다. 칩·배지의 옅은 배경은 24%.

| 키 | 색상 | 계열 | 기본 카테고리 |
|---|---|---|---|
| peach | `#F09A7E` | 주황 | 식비 (고정) |
| sage | `#86BC96` | 초록 | 카페 (고정) |
| rose | `#E791B0` | 분홍 | 쇼핑 (고정) |
| lavender | `#A89FE0` | 보라 | 문화 (고정) |
| sky | `#7FB2E3` | 파랑 | 여행 (고정) |
| mint | `#72BFB2` | 청록 | 교통 |
| butter | `#E6C274` | 노랑 | 생활 |
| lilac | `#C99BDB` | 보라 | 의료/건강 |
| clay | `#C79B7C` | 갈색 | 주거/통신 |
| apricot | `#F0B27A` | 주황 | 경조사/선물 |
| warmgray | `#ADA195` | 회색 | 기타 |
| olive | `#B3C27A` | 초록 | (사용자 추가용 예비) |

- 기본 고정 5개는 계열이 모두 다르다(주황·초록·분홍·보라·파랑). 칩 순서가 무지개처럼 늘어서지 않게 섞었다 — 무지개 순서는 알록달록해 앱 톤과 맞지 않았다.
- 계열은 9가지. 피치·살구, 세이지·올리브, 라벤더·라일락은 같은 계열로 본다.
- 사용자가 추가한 항목은 아무도 안 쓰는 색부터 받는다. 고정으로 올린 항목이 남은 고정 항목과 같은 계열이면, 올린 항목의 색만 다른 계열로 바꾼다.

### 타이포그래피
커스텀 폰트 페어링 사용. 헤드라인·금액은 부드러운 세리프, 본문은 라운드 산세리프로 따뜻한 무드를 만든다.

- **헤드라인 / 금액**: `Gowun Batang` (serif) — fallback `Apple SD Gothic Neo, serif`
- **본문 / 라벨**: `Gowun Dodum` (sans) — fallback `Apple SD Gothic Neo, system-ui, sans-serif`
- (둘 다 Google Fonts, 한글 지원. PNG/PDF 내보내기 시엔 fallback으로 렌더됨)

| 역할 | 크기/줄간격 | 폰트·굵기 | 앱 내 적용 위치 |
|---|---|---|---|
| Display | 28~32 / 1.1 | Batang 700 | 월 선택("2026년 8월"), 예산 대표 금액 |
| Headline | 20~24 / 1.3 | Batang 700 | 앱 타이틀("같이살림"), 섹션 제목 |
| Title | 21 / 28 | Batang 700 | 카드 내 강조 수치 |
| Body | 15~16 / 1.5 | Dodum 400 | 리스트 아이템 본문(상호명 등), 금액 |
| Caption | 12~13 / 1.4 | Dodum 400 | 보조 정보(카테고리·지출자·날짜 메타) |
| Label | 11 / 1.4 | Dodum 400~700 | 하단 탭 라벨, 칩 라벨 |

- 세리프(Batang)는 제목·금액 등 "눈에 띄어야 하는" 곳에만, 나머지 본문은 Dodum으로 가독성 우선.

### 형태
- 모서리: 카드 24 / 작은 카드·요약 18 / 아이콘 칩 12~14 / 칩·진행률 바 99(pill)
- 그림자: 부드러운 확산 그림자 `0 8px 24px -12px rgba(120,90,70,.20)` + `0 2px 6px -3px rgba(120,90,70,.10)` (하드 보더 대신 그림자로 카드 분리)
- 아이콘: 스트로크 기반 라인 아이콘(24px 그리드, stroke ~1.8~2), 한 가지 스타일로 통일. 앱 코드에서는 Material Symbols(Outlined)로 대응.

### AI 디자인 툴 전달 시 참고
Stitch, Claude Design 등에 와이어프레임과 함께 전달할 때는 이 섹션(1번) 전체 + `docs/prd.md` + `docs/wireframe/`을 함께 제공한다. 색상/컴포넌트 규칙을 프롬프트에 명시적으로 포함시켜야 AI 디자인 툴이 임의로 스타일을 정하지 않는다.

---

## 2. 공통 컴포넌트

화면을 만들면서 계속 채워지는 목록. Material 3 컴포넌트 구조를 쓰되 1번의 파스텔 토큰(컬러·폰트·형태)으로 스타일링한다.

### 하단 탭바
- 구성: 홈 / 가계부 / 일정 / 디데이 / 설정 (5개)
- Material 3 Navigation Bar 기반. 선택 탭은 Coral(`#E8896B`), 비활성은 Muted(`#A2948A`). 라인 아이콘 + 라벨.
- 확정 필요: 5개 탭이 화면에서 복잡해 보이지 않는지 (PRD "남은 결정 사항" 참조)

### 카드 (Card)
- 쓰임: 예산 카드, 카테고리 카드, 디데이 카드, 최근 지출 카드, 연결 상태 카드 등
- 흰색(`#FFFFFF`) 배경 + 라디우스 24(작은 카드 18) + soft diffuse shadow(1번 "형태" 참조). 하드 보더는 지양.

### 칩/태그 (Chip)
- 쓰임: 카테고리 선택, 지출자 선택, 일정 유형(우리/개인) 필터, D-day 배지, 예산 입력 빠른 금액 버튼(`+1만`~`+100만`)
- pill(라디우스 99) 형태. 선택/강조 칩은 Coral 배경 + 흰 텍스트, 옅은 칩은 Primary Soft(`#FCE4D8`) 배경 + Coral 텍스트.
- 한 줄에 여러 개를 균등 폭으로 깔 때(예산 빠른 금액 5개)는 `Modifier.weight(1f)` + `SalimChip`의 `horizontalPadding`/`textStyle` 축소 파라미터를 쓴다. 개수가 고정이고 항상 다 보여야 하는 경우에 한하며, 글꼴 확대 시 라벨이 잘릴 수 있음을 감수한다.
- 라벨 길이가 제각각인 칩 묶음(카테고리)은 개수로 끊지 않고 `ChipFlowRow`로 폭 기준 줄바꿈한다.
- 카테고리 칩은 `CategoryChip`을 쓴다. 라벨 앞에 카테고리 아이콘(18dp)을 붙이고, **미선택은 아이콘 배지(`CategoryIconBadge`)와 같은 톤** — 카테고리색 24% 배경 + 카테고리색 아이콘 + 본문색(`#4B403A`) 글자. 파스텔은 글자색으로 쓰면 읽히지 않아 글자는 본문색으로 둔다.
- **선택도 같은 카테고리색을 쓴다** — 배경 40% + 같은 색 1.5dp 테두리 + 굵은 본문색 글자. 다른 색(Coral)으로 갈아타면 고른 순간 카테고리 색이 사라져 어색하다. 테두리는 칩 안쪽에 그려 선택해도 크기가 변하지 않는다. "+더보기"처럼 카테고리가 아닌 칩은 일반 옅은 칩(Primary Soft + Coral)을 쓴다.

### 리스트 아이템 (List Item)
- 쓰임: 최근 지출 내역, 전체보기 지출 리스트, 일정 리스트, 설정 메뉴
- 공통 패턴: 아이콘 + 텍스트(Body Medium/Body Small) + 우측 값 또는 화살표
- 변형: 좌측 색상 바 있는 버전(일정), 없는 버전(지출)

### 플로팅 액션 버튼 (FAB)
- 쓰임: 가계부(전체보기), 일정 화면 우측 하단 "+" 버튼
- Material 3 FAB 기본형

### 하단 고정 CTA 버튼
- 쓰임: 지출 입력/수정 "저장하기", 온보딩 "다음/시작하기"
- Material 3 Filled Button, 화면 하단 폭 100%

### 진행률 바 (Progress Bar)
- 쓰임: 예산 대비 지출 진행률
- 트랙 `#F1E7DB`, Fill은 Peach→Coral 그라데이션, 라디우스 99(pill)
- 확정 필요: 예산 초과 시 색상 변경 규칙 (테라코타 계열 경고색 최소 적용)

### 소셜 로그인 버튼 (예외 규칙)
- 쓰임: 로그인 화면 "Google로 계속하기"
- **파스텔 토큰을 적용하지 않는 유일한 버튼.** 구글 브랜드 가이드라인상 흰 배경 + 공식 G 마크 + 회색 테두리(`#DADCE0`)를 유지해야 하므로, 앱 톤보다 브랜드 규칙이 우선한다.
- 라디우스 12, 높이 54(하단 CTA와 동일). 로딩 중에는 라벨 대신 Coral 스피너.
- 카카오/애플 추가 시에도 각 브랜드 가이드라인을 따르고, 버튼 크기·간격만 이 항목에 맞춘다.

### 코드 입력 필드 (Code Input)
- 쓰임: 상대방 연결 - 초대 코드 입력 (connect.md 9-3)
- 6칸 분할 입력. 칸마다 라디우스 12 박스, 채워진 칸은 Coral 테두리 + 헤드라인 서체(Gowun Batang)로 글자 강조
- 한 글자 입력 시 다음 칸으로 자동 이동, 지울 때 이전 칸으로 복귀. 입력값은 대문자로 정규화
- 오류 상태: 테두리를 경고색(테라코타)으로 바꾸고 아래 문구 노출. 문구 자리는 비어 있을 때도 유지해 레이아웃이 밀리지 않게 한다

### QR 카드 (QR Card)
- 쓰임: 상대방 연결 - 내 초대 코드 (connect.md 9-2)
- 흰 카드(라디우스 24) 위에 QR을 여백과 함께 배치. QR 자체는 흑백 유지 — 파스텔 톤을 입히면 인식률이 떨어진다
- **QR 아래에 코드 원문을 병기**한다 (스캔이 안 될 때 손입력으로 넘어갈 수 있도록)
- 코드는 끊어 쓰지 않고 자간으로 읽힌다 — 옮겨 적는 값이라 공백이 보이면 코드의 일부로 읽힌다

### 페이지 인디케이터 (Page Indicator)
- 쓰임: 온보딩 슬라이드 위치 표시
- 점 형태(pill). 활성 9dp Coral, 비활성 7dp `#F1E7DB`(Progress Track), 간격 8dp

### 도넛/파이 차트
- 쓰임: 홈 화면 카테고리별 지출
- 색상: 1번 "카테고리 색" 표를 따른다(카테고리마다 고유색), 트랙은 `#F1E7DB`. 가운데는 배경색으로 뚫어 도넛 형태.
- 확정 필요: 카테고리 몇 개까지 차트에 표시하고 나머지는 "기타"로 묶을지

---

## 3. 시안 소스 (디자인 원본)
- 홈 파스텔 시안 원본: [docs/design/home/](docs/design/home/) (`Main.dc.html`, `canvas.json`, `salim-home-pastel.html`)
- 온라인 캔버스(열람·수정·PNG/PDF 내보내기): docs/design/home/README.md의 Artifact 링크 참조
- 이 문서(1번 섹션)의 토큰 값은 위 시안에서 추출한 것이다. 시안과 문서가 어긋나면 **시안이 우선**이며 문서를 맞춘다.

---

## 사용 방법
1. 새 화면을 코드로 만들 때, 1번 섹션(디자인 시스템/컬러/타이포/형태)을 항상 기준으로 삼는다.
2. 위 컴포넌트 목록의 요소는 Material 3 구조를 쓰되 1번의 파스텔 토큰으로 스타일링한다. 임의 색·폰트·크기를 새로 만들지 않는다.
3. 새로운 반복 패턴이 3회 이상 나타나면 컴포넌트 목록에 항목을 추가한다.

---

## 변경 로그
- 2026-09-13: **카테고리 색을 카테고리마다 고유색으로** — 파스텔 세트 5색만으로는 고정 항목끼리 색이 겹칠 수 있어 12색 팔레트("카테고리 색")로 확장. 색은 아이콘에서 분리해 카테고리 문서의 `color`로 저장한다. 피치·코랄(주황), 세이지·민트(초록)처럼 키만 다르고 눈에 같은 색이 고정에 함께 있던 문제로, 겹침 판정을 **색 계열** 기준으로 한다. 이어서 카테고리 차이가 약하다는 피드백에 따라 칩·배지 배경을 16% → 24%로 올리고, 팔레트를 **한 단계 진한 파스텔 12색**으로 교체했다(고정 5개 = 피치·세이지·로즈·라벤더·스카이, 계열이 모두 다르고 순서는 섞음). 중간에 시도한 무지개 계열은 채도가 높아 앱 톤과 맞지 않아 되돌렸다. 앱 공통 파스텔 토큰(Peach·Sage·Lavender·Mint)은 진행률 바 등에 그대로 둔다.
- 2026-09-13: 카테고리 칩 색을 코랄 단색에서 **카테고리색 옅은 배경 + 색 아이콘**으로 변경(`CategoryChip`) — 수정 화면·가계부 리스트의 아이콘 배지와 톤을 맞추기 위해. 선택 상태도 Coral 대신 같은 카테고리색(배경 40% + 테두리)으로 표시 — Coral로 바뀌는 게 어색하다는 피드백.
- 2026-09-13: 카테고리 수정(PRD 7) 구현에 맞춰 칩 규칙 추가 — 폭 기준 줄바꿈(`ChipFlowRow`), 카테고리 칩 앞 아이콘(글자색과 같은 색). 가계부 리스트의 카테고리 아이콘 배지를 `CategoryIconBadge`로 공용화해 카테고리 수정 목록에서도 재사용.
- 2026-08-30: 상대방 연결(PRD 9) 구현에 맞춰 **코드 입력 필드**, **QR 카드** 항목 추가. 코드는 끊어 쓰지 않고 자간으로 읽히게 하는 규칙을 함께 못박음 — 옮겨 적는 값이라 공백이 코드의 일부로 읽힌다.
- 2026-08-23: 로그인/온보딩 구현에 맞춰 **소셜 로그인 버튼(브랜드 가이드라인 예외)**, **페이지 인디케이터** 항목 추가.
- 2026-08-19: 미니멀 브라운 테마 → **소프트 파스텔**로 전환. 홈 파스텔 시안 기준으로 컬러/타이포(Gowun Batang·Dodum)/형태 갱신, 시안 소스를 `docs/design/home/`에 보관. (앱 코드 테마 토큰 반영은 별도 작업)
- 2026-08-23: 예산 입력 시트에 만원 단위 빠른 금액 버튼(누적) 5개를 한 줄 균등 폭으로 추가, 지우기는 금액 우측 아이콘으로 분리 — 옅은 칩 스타일 재사용(`horizontalPadding`/`textStyle` 축소 파라미터 추가), 새 컴포넌트는 만들지 않음.
