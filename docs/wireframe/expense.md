# 4. 가계부

가계부 탭은 별도 대시보드 없이, 아래 "전체보기" 화면이 탭의 기본(랜딩) 화면 역할을 한다.

## 4-1. 전체보기 (탭 랜딩 화면)

### 화면 목적
이번 달 지출을 날짜순으로 훑어보고, 새 지출을 빠르게 추가하는 화면.

### 레이아웃 (위→아래 순서)
1. **상단 - 월 선택 + 액션 아이콘**
   - 좌: "2026년 8월" + 드롭다운 (월 변경)
   - 우: 검색 아이콘, 필터 아이콘
2. **월 합계 카드**
   - "이번 달 총 지출" 라벨 + 금액
3. **날짜별 그룹 리스트**
   - 날짜 헤더 (예: "8월 5일 (화)") 아래로 해당 날짜의 지출 항목 나열
   - 항목 구성: 카테고리 아이콘, 상호명/메모, 카테고리·지출자 메타 정보, 금액
   - 항목 탭 시 지출 수정 화면으로 이동
4. **플로팅 액션 버튼 (+)**
   - 화면 우측 하단 고정, 탭 시 지출 입력 화면으로 이동
5. **하단 탭 내비게이션** (전 화면 공통, "가계부" 강조)

### 필터/검색 (아이콘 탭 시 노출되는 패널, 별도 화면 여부는 확정 필요)
- 필터: 기간, 카테고리, 지출자 (조합 가능)
- 검색: 메모 텍스트 기반

### 상태 분기 (추후 확정 필요)
- 해당 월 지출 데이터 없음: 리스트 영역에 "이번 달 지출이 없어요" 빈 상태 노출
- 필터 적용 후 결과 없음: "조건에 맞는 지출이 없어요" 문구 + 필터 초기화 버튼

---

## 4-2. 지출 입력

### 화면 목적
새로운 지출 1건을 빠르게 기록.

### 레이아웃 (위→아래 순서)
1. **상단 - 타이틀만 노출** ("지출 입력"), 좌측에 닫기(X) 아이콘. 별도 저장 버튼은 상단에 두지 않음 (하단 버튼과 중복 방지)
2. **금액 강조 영역**
   - 화면 중앙 상단, 큰 숫자로 금액 표시/입력
3. **입력 필드 리스트** (구분선으로 구획)
   - 날짜 (기본값: 오늘)
   - 시간
   - 지출자 — "나" / "배우자" 2개 칩 중 택1
   - 카테고리 — 칩 형태, 자주 쓰는 카테고리 노출 + "+더보기"로 전체 목록 확장
   - 메모 — 텍스트 입력
4. **하단 고정 버튼**: "저장하기"

### 상태 분기 (추후 확정 필요)
- 금액 미입력 시 저장 버튼 비활성화 여부
- 카테고리 목록이 많아질 경우 "+더보기" 확장 UI (바텀시트 vs 전체 화면)

---

## 4-3. 지출 수정

### 화면 목적
기존 지출 1건의 내용을 수정하거나 삭제.

### 레이아웃
- **지출 입력과 동일한 레이아웃을 재사용**, 기존 값이 프리필된 상태로 진입
- 상단: 좌측 뒤로가기 아이콘, 우측 삭제(휴지통) 아이콘으로 교체 (닫기 아이콘 대신)
- 하단 고정 버튼 라벨만 "수정 완료"로 변경

### 삭제 플로우
1. 상단 삭제 아이콘 탭
2. 확인 팝업 노출 ("삭제하시겠어요?")
3. 확인 시 삭제 처리 → 이전 화면(전체보기)으로 복귀
4. 취소 시 팝업만 닫힘, 수정 화면 유지

### 상태 분기 (추후 확정 필요)
- 삭제 후 복원(실행취소) 기능 제공 여부
